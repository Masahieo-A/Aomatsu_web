# 構成メモ（開発者向け）

- **GitHubリポジトリ**: 未設定（`git remote get-url origin` で確認したところ remote 未登録。AGENTS.md 記載の通り GitHub/Vercel は所有者が手動作成・Import、push は SSH のみ）
- **フレームワーク**: Next.js 16.2.6（App Router・TypeScript）+ React 19.2.4。認証は next-auth ^5.0.0-beta.31（@auth/core）。スタイリングは Tailwind CSS ^4、shadcn ^4.7.0。画像生成は openai ^6.38.0（`gpt-image-1`）。（package.json より確認）
- **DB / データ管理**: Supabase（`@supabase/supabase-js` ^2.106.1 を使用）。`lib/supabase.ts` でクライアントを管理。認証方式は NextAuth.js（Auth.js v5）+ Google OAuth Provider、JWTセッション戦略（`auth.ts` より確認）。学校ドメイン制限は `ALLOWED_EMAIL_DOMAIN` 環境変数で実施。
- **APIキー管理**: `.env.local`（gitignore対象）と `.env.example`（テンプレート、git管理下）が存在。含まれるキー名: `NEXTAUTH_URL`, `NEXTAUTH_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `ALLOWED_EMAIL_DOMAIN`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `OPENAI_API_KEY`, `SYSTEM_PROMPT_TEMPLATE`, `MAX_GENERATIONS_PER_USER`（値は未確認・記載なし）。本番はVercel環境変数で管理（.vercel/project.json が存在するためデプロイ済みと確認）。
- **デプロイ先**: Vercel（`.vercel/project.json` より projectName: `image_2.0`, projectId: `prj_c92R3MT7JGHDdSR45ufDC9xNA4D6`, orgId: `team_48IzfiiYdsd5KvTIG7Mx7ELc`）
- **関連ドキュメント**: 機能要件は `docs/要件定義.md`
