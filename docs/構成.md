# 構成メモ（開発者向け）

- **GitHubリポジトリ**: `Masahieo-A/Aomatsu_web`（`git remote get-url origin` で確認: `git@github.com:Masahieo-A/Aomatsu_web.git`）
- **フレームワーク**: モノレポ構成。ルートに package.json なし（静的ポータル、`index.html` を直接配信）。`apps/` 配下に Next.js アプリが2つ:
  - `apps/app-eisaku-tensaku`: Next.js 15.3.8（App Router） / React 19 / TypeScript / Tailwind CSS 4 / `@google/genai`（Gemini API）
  - `apps/app-cloze-seijo-maker`: Next.js 15.3.8（App Router） / React 19 / TypeScript / Tailwind CSS 4
  - `apps/app-elsa-like`: 静的HTML / Vanilla JS（package.json なし）
- **DB / データ管理**: `@supabase/supabase-js` は現時点でどのアプリの package.json にも未導入（未確認＝依存追加なし）。`apps/app-cloze-seijo-maker` はドキュメント上「Supabase予定」と記載されているが、実装・依存関係は未確認（配線整備中とのコメントあり）。他アプリにDB接続なし。
- **APIキー管理**:
  - `apps/app-eisaku-tensaku/.env.local` と `apps/app-eisaku-tensaku/.env.example` が存在。含まれるキー名: `GEMINI_API_KEY`（値は確認・記載せず）。
  - `apps/app-cloze-seijo-maker` に `.env*` ファイルは未確認（見つからず）。
  - 本番はVercel環境変数（`GEMINI_API_KEY` をVercel Dashboardの Environment Variables に設定、docs/deployment.md記載）。
- **デプロイ先**: ルート直下 `.vercel/project.json` に `projectId: prj_UnbSysr32QxxsiJA7Rglr2s56AQA`, `orgId: team_48IzfiiYdsd5KvTIG7Mx7ELc`, `projectName: aomatsu-english-portal` を確認（ポータルの本番プロジェクト）。ルート `vercel.json` はセキュリティヘッダ設定のみ（ビルド設定なし、静的配信）。各サブアプリのVercelプロジェクト名・URLは `docs/deployment.md` に記載あり（`eisaku-tensaku-app`, `cloze-seijo-maker`, `elsa-like`）。
- **関連ドキュメント**: 機能要件は `docs/要件定義.md` を参照。デプロイ対応表・環境変数一覧は `docs/deployment.md`、実装ルールは `docs/coding-rules.md`、デザインは `docs/design-system.md`、Claude Code作業ルールは `docs/prompt-rules.md` を参照。
